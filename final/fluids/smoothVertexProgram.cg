void main (
	float4 position : POSITION,
	out float2 coord: TEXCOORD0,
	out float4 pos : POSITION
)
{
	float2 scale = float2(0.5, 0.5);
	coord = float2(position) * scale + scale;
	pos = float4(position.xy, 0.0, 1.0);
}